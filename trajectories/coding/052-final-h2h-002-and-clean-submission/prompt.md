# Task prompt

This trajectory records the work authorized by two pasted instruction files
supplied with this task. The filenames were `pasted-text-1.txt` and
`pasted-text-2.txt`; their contents were read before work began.

Summary of the direct instruction: run the final, sealed H2H-002 benchmark
comparing native single-thread chat, augmented raw memory, and the frozen
Blackhole Product V2 on three fresh synthetic worlds; preserve H2H-001 and all
protected benchmark boundaries; document reproducible results; then perform
the final repository cleanup, move submission-only human materials outside
the repository, create a clean tracked-HEAD submission ZIP, verify it with
offline checks, commit the scoped changes to `master`, and stop. Do not modify
frozen product behavior, benchmark ground truth, protected holdout/V1 oracle
material, or the frozen product tag.
