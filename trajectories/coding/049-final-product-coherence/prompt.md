# Task prompt

Source: the user message that initiated this task, received 2026-08-31.

```text
/goal Referenced pasted text files:
- pasted text file: /Users/natan/.codex/attachments/0f0f2654-1189-48d0-b29a-aff7649bfaef/pasted-text-1.txt. Read this file before continuing.
```

The referenced pasted text was read in full before implementation. It is the
direct task specification titled “BLACKHOLE — FINAL PRODUCT COHERENCE + DEMO
READY GATE”. It authorizes the final scoped Product V2 coherence work directly
on `master`, beginning at `e71fa2190835063598fdcbc33bfc5835d3dabc25`, with a
clean worktree and `origin/master` at the same SHA. It requires preserving the
frozen benchmark/evaluator/V1 boundaries, completing the Ask provider contract,
generic Attention lifecycle reconciliation, useful document identity,
Attention and processing layout fixes, deterministic regression coverage, one
bounded synthetic live-provider scenario, browser review, full repository
gates, trajectory evidence, a coherent commit, a normal push, and the final
submission tag only after a full pass.

The specification forbids benchmark runs, model/reasoning/batch tuning, V1
oracle access, private dogfood data in Git, OCR, noun translation tables,
provider-owned persistence mutation, and hard-coded VAT/children/document-
number rules. It requires raw evidence, provenance, rebuildability, unknown
semantics, and safe degraded behavior to remain intact.
