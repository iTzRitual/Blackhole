# Product V2 master consolidation and final dogfood fixes

## Initiating instruction

The human instruction was supplied as the full contents of the pasted text
file below and was read before this task began:

`C:\Users\natan\.codex\attachments\49c0b8c9-c0d2-401a-9e4b-bee6647c9bf0\pasted-text-1.txt`

This file is the authoritative task record; this trajectory entry is an
honest summary of that instruction rather than a fabricated transcript.

## Authorization summary

Consolidate the exact Product V2 source branch
`product/v2-final-dogfood-fixes` at
`602bc9848af1da02db1ee5809fb321aa9173959f` into a clean `master` at
`68b7b15d353b12cffb65a770f8583aa0ebb849dd` using history-preserving Git
integration. Verify the Product V2 coding lineage, run the provider-free
integration gates, push the consolidated `master` without force, and then
continue directly on `master` without creating another branch, worktree, or
parallel coding agent.

Implement the final generic dogfood corrections: canonicalize duplicate
Attention projections, remove completed/cancelled/superseded items from active
Attention and its badge, make countdowns update without refresh, preserve
capture-time temporal anchoring, differentiate approaching and overdue states,
replace internal/raw fields with useful human explanations, make Memory
current-first with contextual history and natural desktop layout, distinguish
coexisting occurrence events from competing state facts, aggregate numeric
occurrences deterministically, explain unresolved conflicts naturally, prevent
raw structured objects from reaching users, add bounded temporary Ask thread
context and a New thread action, keep the Ask composer usable while scrolling,
show examples only on an empty thread, and replace the eye processing indicator
with a reduced-motion-safe neutral loader.

Add deterministic regressions for Attention, Memory, occurrence semantics and
aggregation, Ask thread context, raw-object prevention, and the UI contracts.
Run the full specified regression and acceptance/evaluation gates, preserve
the frozen benchmark/evaluator/baseline and holdout boundaries, record a new
trajectory and evaluation evidence, commit directly on `master`, push if
authenticated, and stop. No Monster-specific or hairdresser-specific rules,
provider-performance work, provider configuration changes, benchmark changes,
V1 oracle/scoring access, or G01/G02/G03 use are authorized.
