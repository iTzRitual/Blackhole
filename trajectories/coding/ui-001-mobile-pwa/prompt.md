# UI/PWA Workstream Prompt

This is a summary of the directly authorized instruction, based on the user-referenced pasted goal file at `C:\Users\natan\.codex\attachments\be0f0926-d42e-43cc-92b9-0531e14c2e1f\pasted-text-1.txt` (not a verbatim transcript):

Implement the Blackhole mobile-first UI/UX and installable PWA workstream. Work only in a sibling git worktree created from the current primary HEAD, on branch `ui/mobile-pwa`; do not modify, merge into, or rebase the primary backend worktree. Evolve the existing static frontend under `app/web/`, preserve the existing local API, and keep the scope to the capture, attention, memory, ask, attachment interaction, milestone, accessibility, responsive, and PWA shell experience. Do not change benchmark, evaluator, semantic backend, StateStore, relation recovery, completeness, or experiment-result files. Document the work, validate required viewports and PWA assets, commit only the UI branch, and provide a handoff with limitations and exact run instructions.

