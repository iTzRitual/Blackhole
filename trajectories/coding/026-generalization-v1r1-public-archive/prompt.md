# Initiating instruction

Verbatim user instruction:

> /goal Referenced pasted text files:
> - pasted text file: C:\Users\natan\.codex\attachments\c991df89-5452-40d3-881c-c334fa8cbc17\pasted-text-1.txt. Read this file before continuing.

Task authorization summary (from the referenced pasted text): consolidate the
public-safe Generalization V1R1 evidence into a clean archive based on the
current remote `master`, preserve the specified branch tips as annotated tags,
merge only the two sealed public-safe V1R1 candidate branches, copy and inspect
only the listed public-safe scoring artifacts, make minimal evidence/index
documentation updates, verify hashes/ancestry/runtime invariants, publish the
archive directly to `origin/master`, push and verify tags, delete only the
specified obsolete remote branches, retain the oracle and scoring worktrees,
and return the required gate report. Runtime behavior and benchmark semantics
must remain unchanged; oracle expected answers and oracle/scoring Git history
must not enter the public repository.
