# Retrospective task instruction

> This is a retrospective summary of the human-authorized scaffold task, not a verbatim historical prompt. The original work predates the trajectory protocol, and no authentic transcript was available.

## Authorized scope

The human authorized a documentation-only repository scaffold for the micro1 Agentic Workflows Hackathon project, **Life Inbox**: a zero-organization personal inbox where a person can capture fragmented everyday information without classifying it first.

The authorized scaffold task covered:

- documenting the product concept and its intended downstream responsibilities;
- documenting immutable raw sources, rebuildable derived state, known/inferred/unknown semantics, deterministic calculations, explicit approval, and benchmark isolation;
- creating the repository layout for documentation, prompts, benchmark development and holdout boundaries, baselines, future application work, data, evaluation results, trajectories, and scripts;
- adding documentation and directory placeholders only; and
- presenting the proposed structure before making changes, then making one coherent git commit after human approval.

The task explicitly did not authorize application code, benchmark cases, benchmark expected outputs or ground truth, evaluation code, or infrastructure.
