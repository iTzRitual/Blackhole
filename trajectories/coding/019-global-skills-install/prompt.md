# Prompt

The user asked to install the two supplied skill bundles globally rather than in the current project. The two install commands were recorded with their access tokens redacted here to avoid committing credentials.

```text
npx @animationsdev/install --token=<redacted>
npx @aiforui/install --token=<redacted>
```
