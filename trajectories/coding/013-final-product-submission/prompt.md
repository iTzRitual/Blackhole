# Retrospective task instruction

This is a retrospective summary of the human-authorized final Blackhole
product and hackathon-submission task, not a verbatim historical prompt.

The validated experimental core is complete: Gate A remains frozen at the
200-event benchmark with checkpoints 50/100/150/200; Gate B and
`response-contract-v2` remain valid; `baseline-v1` remains the official
baseline; and the genericity repair is KEEP. Turn the experimental core into
a complete, reproducible, video-ready hackathon submission. First inspect the
recorded extraction/state for relation-detail evidence and allow at most one
bounded deterministic relation experiment only if all missing details are
already present. Otherwise document that richer extraction is deferred and
continue.

Build the smallest coherent mobile-first Blackhole web demo with universal
capture, attention, current state/memory, and Ask Blackhole views. Add a small
deterministic synthetic demo seed and reset command, preserve the
subscription-first Codex CLI boundary without reading credentials, create
representative runtime trajectories, audit reproduction commands, create a
concise final comparison artifact, polish the README, and add a five-minute
video script and shot list. Do not reopen Gate A, change benchmark facts,
expected outputs, `response-contract-v2`, baseline-v1, or add holdout access.
Do not fabricate transcripts or evidence, and stop after the final Gate C
validation and report.
