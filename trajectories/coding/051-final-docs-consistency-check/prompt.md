# User instruction

BLACKHOLE — FINAL DOCS CONSISTENCY CHECK ONLY

Documentation-only work. Do not perform product development, rerun the
benchmark, make provider calls, or change the app. Audit the capture-success
copy, final Product V2 Ask contract, and PTS naming in the sealed H2H
definition, and read `docs/FINAL_VIDEO_SCRIPT.md` end-to-end. Correct the
committed documentation so it describes `Out of mind`, the normal provider-
rendered READY Ask path with deterministic responsibilities and a degraded
fallback only when needed, and the exact sealed PTS expansion. Preserve all
formulas, scores, cases, expected outputs, benchmark artifacts, semantic
interpretation, and the frozen product tag. Run `git diff --check`, confirm
documentation-only paths, commit directly to `master`, push, and report the
changed docs and final/tag SHAs.
