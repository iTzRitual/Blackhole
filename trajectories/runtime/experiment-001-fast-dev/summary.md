# Runtime trajectory summary: Experiment 001 FAST initial attempt

This is a failed, non-official diagnostic run. It used the public first 50
events and the Experiment 001 extraction prompt with Codex CLI at the default
`max` semantic reasoning setting. The call exceeded the 900-second timeout and
was manually stopped before a checkpoint query or score completed.

The prompt and the raw partial/returned extraction output are retained in
`calls/`. The output was useful as a recorded semantic input for later
projection diagnostics; it is not a benchmark result. No expected output,
holdout material, or provider credential was supplied or written.
