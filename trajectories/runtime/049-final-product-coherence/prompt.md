# Runtime trajectory prompt

This representative runtime execution records the bounded synthetic live
provider and browser dogfood required by the final Product V2 coherence and
demo-ready gate. It uses a fresh temporary Home, no private content, at most
six captures and eight Ask requests, and the already-installed authenticated
local Codex CLI through the normal subscription-first path. It verifies
document identity, generic Attention lifecycle reconciliation, explicit Done,
restart/rebuild persistence, provider-backed READY Ask, and the 390×844 and
1280×900 UI geometry review.
