# Runtime trajectory summary: Codex size calibration

This directory contains actual final provider messages and a sanitized summary
of the non-scored Gate A runtime calibration. It is not an authentic exported
provider transcript, and no transcript has been fabricated. The calibration
oracle was not supplied to any provider session.

## Input and state before execution

- Provider instruction: frozen [`baseline-v1`](../../../prompts/runtime/baseline-v1.md).
- User input: one complete chronological JSONL history at each of 50, 100, 200,
  and 400 events, generated by the separate calibration generator.
- Follow-up input: the fixed query bundle, sent by resuming the same provider
  session.
- Blackhole state before execution: none. No application database, hidden
  summary, retrieval result, expected output, or evaluator artifact was present
  in the temporary workspace.
- Provider/tool behavior: read-only policy; no tools were invoked by the
  baseline; no consequential action was requested or performed.

## Runtime configuration

| Field | Value |
| --- | --- |
| Provider | Codex CLI `codex-cli 0.150.0-alpha.12.2` |
| Model | `gpt-5.6-luna` |
| Reasoning effort | `max` |
| Session strategy | Fresh persistent session per size, then `codex exec resume` |
| Workspace | Fresh isolated temporary directory per size |
| Context result | All full histories completed; no context-warning or truncation signal |
| Auth | External CLI authentication; only safe authenticated status was recorded |

## Session and output records

| Events | Provider thread ID | Initial output | Query output | Initial input/output/reasoning tokens | Query input/output/reasoning tokens | Runtime |
| ---: | --- | --- | --- | ---: | ---: | ---: |
| 50 | `01a04abe-cb29-7fe2-a146-ed3c3d1e9b3c` | [`initial-050-official.txt`](initial-050-official.txt) | [`query-050-official.txt`](query-050-official.txt) | 21,490 / 33 / 22 | 24,696 / 9,803 / 9,305 | ~179 s* |
| 100 | `01a04ac2-7dd6-7552-a735-71e409797f35` | [`initial-100.txt`](initial-100.txt) | [`query-100.txt`](query-100.txt) | 28,138 / 36 / 25 | 31,347 / 9,997 / 9,500 | 186.975 s |
| 200 | `01a04ac7-a423-72a2-b225-c6115a304f0c` | [`initial-200.txt`](initial-200.txt) | [`query-200.txt`](query-200.txt) | 41,438 / 42 / 31 | 44,653 / 15,003 / 14,502 | 277.032 s |
| 400 | `01a04acc-3bc5-7fe0-ae01-13dc1debe956` | [`initial-400.txt`](initial-400.txt) | [`query-400.txt`](query-400.txt) | 68,121 / 40 / 29 | 71,334 / 31,579 / 31,078 | 575.956 s |

\* The clean 50-event end-to-end runtime is approximate from the official
trajectory file timestamps; its usage values came from the completed wrapper
result. The initial response in each file is `CAPTURES_RECEIVED`. The query
files contain the final JSON response only; process JSONL stdout was withheld
from the user-facing artifact.

## Observable decisions and verification

1. The CLI accepted the requested model and reasoning setting in a harmless
   ephemeral probe.
2. A separate persistent-session probe completed and resumed successfully.
3. Each calibration run used the same provider, model, reasoning setting,
   baseline prompt, query bundle, and read-only/no-tools policy.
4. The controller compared final JSON responses with the visible,
   calibration-only oracle after the run. This was a one-off deterministic
   calculation, not a committed evaluator implementation.
5. The fixed query bundle correction was applied before the clean official
   50-event run; the earlier pilot is stored as `initial-050.txt` and
   `query-050.txt` for diagnostic provenance only and is not scored.

## Resulting state-quality readout

| Events | Current | Previous | Missing/unknown | Relations | LQA-style | State defects |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 50 | 1.0000 | 0.6667 | 1.0000 | 0.6000 | 0.8167 | 2 previous |
| 100 | 1.0000 | 1.0000 | 1.0000 | 0.6000 | 0.9000 | none |
| 200 | 1.0000 | 0.8182 | 1.0000 | 0.6000 | 0.8545 | 1 previous |
| 400 | 0.8182 | 0.8182 | 1.0000 | 0.6000 | 0.8091 | 1 current, 1 previous |

The relation score was reduced at every size by overestimating the duplicate
count; the correction, contradiction, and ambiguous-link counts matched. The
400-event run is the relevant practical stress result: it remained accepted but
was slower and had additional current/previous-state defects. No 800-event
extension was run.

## Safety and boundary check

No holdout material was accessed. The runtime workspace did not contain the
repository or calibration oracle. No API key or provider token was requested,
printed, or persisted. No application, baseline, benchmark case, evaluator, or
infrastructure implementation occurred.
